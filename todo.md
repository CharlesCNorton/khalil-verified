# Khalil-Verified: Outstanding Issues

1. Make `ScanVariant` actually constructible — implement variant-aware scansion that uses it
2. Add example and counterexample for `scan_summary_correct`
3. Formalize rhyme (qāfiya)
4. Formalize poem/line structure beyond the `is_full_line` boolean
